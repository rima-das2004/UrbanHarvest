    <!-- Navigation Bar -->
    

    <nav class="navbar navbar-expand-lg bg-body-tertiary shadow">
        <div class="container-fluid navdiv" style="max-width: 2000px;">
          <a class="navbar-brand" href="#" style="padding: 0rem 0rem 1.2rem 0rem;"><img src="/img/logo.png" alt="Bootstrap">
        </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if (currentUser) { %>
                <li class="nav-item">
                  <a class="nav-link active" aria-current="page" href="/supplier"><i class="fa fa-home mx-2"></i> Home</a>
                </li>
              <% } %>
              <% if (!currentUser) { %>
                <li class="nav-item">
                  <a class="nav-link active" aria-current="page" href="/product"><i class="fa fa-home mx-2"></i> Home</a>
                </li>
              <% } %>
              
              
              
              <li class="nav-item">
                <a class="nav-link" href="#"><i class="fa fa-question-circle mx-2"></i>Contact Us</a>
              </li>
              <% if (currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/changePassword"><i class="fa fa-key mx-2"></i>Change Password</a>
                  </li>
              <% } %>
             
              <% if (currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/logout"><i class="fa fa-sign-out mx-2"></i> Log Out</a>
                  </li>
              <% } %>
              <!-- <li class="nav-item">
                <button class="btn btn-sm btn-light theme-toggle-btn" id="themeToggle">
                  <p>Theme</p>
                  <i class="bi" id="themeIcon"></i>
              </button>
              </li> -->
            </ul>
           
              <% if (currentUser) { %>
                <ul>

            <li class="nav-item" style="list-style-type: none; margin-top:15px;">
             
               <a href="/accountDetails/<%= currentUser._id %>" class="nav-link d-flex justify-content-center align-items-center">   
                
                 <div class="img" style="width:45px; height:45px; border-radius:50%; overflow:hidden;">
                  <img src="<%= currentUser.image.url %>" alt="profile picture" style="width: 100%;">
                 </div>
 
                </a>
             

            </li>
     
        </ul>
            <% } %>
   
            
          </div>
        </div>
       
      </nav>